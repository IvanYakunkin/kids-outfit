# KidsOutfit Backend

Интернет-магазин детской одежды KidsOutfit — backend часть проекта.

---

## 📦 Технологии

- Node.js
- NestJS
- TypeORM
- PostgreSQL
- JWT для авторизации
- Swagger для документации API

---

## ⚡ Установка и запуск

1. Установить зависимости:

```bash
cd server
npm install
```

2. Создать файл `.env` на основе `.env.example`:

3. Создать базу данных в PostgreSQL

4. Выполнить миграции командой npm run migration:run

5. Запустить сервер разработки:

```bash
npm run start:dev
```

Swagger документация доступна по адресу: [http://localhost:5000/api](http://localhost:5000/api)

---

## 🛠 Основные команды

- `npm run start` — запуск в продакшн режиме  
- `npm run start:dev` — запуск в режиме разработки (с hot reload)  
- `npm run build` — сборка проекта  
- `npm run lint` — проверка кода на ошибки линтера  
- `npm run test` — запуск тестов
- `npm run migration:generate` — генерация файла миграции
- `npm run migration:run` — запуск миграции
- `npm run migration:revert` — отмена последней миграции

---

## 🔐 Авторизация

- Для некоторых эндпоинтов требуется Bearer Token (JWT)
- Токен можно получить через эндпоинт `auth/login`
- Добавляйте токен в заголовок `Authorization: Bearer <token>` для запросов

---

## 📌 Особенности backend

- Управление товарами, заказами и пользователями и др.
- Расчёт итоговой суммы заказов  
- Автоматическое обновление `sold` при заказе  
- Pagination, фильтры и поиск по заказам и товарам  
- Swagger для документации API

## 📌 Основные сущности и модули

- **Users** — управление пользователями, регистрация, авторизация, роли (админ/пользователь).
- **Products** — управление товарами.
- **Sizes** — Размеры одежды.
- **ProductSizes** — размеры товаров, доступность, количество.
- **Orders** — заказы: состав, статус, итоговая сумма, связь с пользователем.
- **Statuses** — статусы заказов.
- **Auth** — JWT авторизация, логин, регистрация, выход из аккаунта и обновление токенов.
- **Categories** — иерархические категории товаров.
- **Cart** — Корзина пользователя.
- **Characteristics** — Характеристики продукции.
- **ProductCharacteristics** — Связь характеристик с товарами.


Каждый модуль содержит CRUD эндпоинты и бизнес-логику для своей сущности.

---

